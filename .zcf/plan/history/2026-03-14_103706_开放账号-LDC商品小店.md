# 任务：开放账号页新增 LDC 商品小店

- 记录时间：2026-03-14 10:31:54 CST
- 页面：`/redeem/open-accounts`
- 目标：管理员可配置小店商品与交付方式（页面展示/邮件发送），购买后订单与交付详情完整留痕，链路闭环。

## 执行计划

1. 扩展数据库与商品模型
- `purchase_products` 增加 `category`、`delivery_mode`
- 新增 `purchase_product_items`（条目库存池）
- 新增 `ldc_shop_orders`（LDC 小店订单与交付留痕）

2. 扩展后台管理能力
- 扩展商品 CRUD：支持分类/交付方式
- 新增条目池 CRUD：新增、编辑、删除、列表

3. 新增用户侧小店链路
- 新增 `/api/open-accounts/shop/*`：商品列表、下单、订单列表、订单详情
- 复用 Credit 支付；支付后自动交付
- 支持 `inline/email/both` 三种交付模式

4. 前端页面接入
- `/redeem/open-accounts` 新增小店区块
- 支持下单、拉起支付、轮询订单、展示交付内容

5. 文档与配置
- 更新 `docs/features/开放账号.md`
- 更新 `docs/features/README.md`
- 更新 `backend/.env copy.example` 注释

6. 验证
- 后端 `node --check`
- 前端 `npm run build`
