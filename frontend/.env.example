# 前端环境变量配置
# 复制此文件为 .env.production 并修改对应值

# API 服务地址（可选）
# 如果不设置，生产环境会自动使用相对路径 /api
# 开发环境会使用 http://localhost:3000/api
# 示例：
# 留空则使用默认配置（推荐）
VITE_API_URL=

# Cloudflare Turnstile 站点密钥（可选）
# 设置后候车室加入队列时会触发人机验证
VITE_TURNSTILE_SITE_KEY=

# Turnstile 小部件尺寸
# - 默认 invisible
# - 如果 Cloudflare 站点密钥开启 Managed Mode，可设置为 managed（将由 Cloudflare 自动控制）
# - 也可手动指定 compact / normal / flexible
VITE_TURNSTILE_WIDGET_SIZE=invisible
